"use strict";

// Class constructor for creating a book object
export class Task {
  constructor(name, dueDate, priority, location) {
    this.name = name;
    this.dueDate = dueDate;
    this.priority = priority;
    this.location = location;
  }
}
